<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/order.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>

            <% if (orders.length <= 0) { %>}
            <h1>Nothing there!</h1>
            <% } else { %>
                <h1>Orders for User#<%= orders[0].userId %></h1>
                <hr>
                <ul class="order-list">
                    <% orders.forEach(order => { %>
                        <li class="order">
                            <h1>Order#<%= order.id %></h1>
                            <ul>
                                <% order.products.forEach(product => { %>
                                    <li class=""><%= product.title %> (<%= product.orderItem.quantity %>)</li>
                                <% }); %>
                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
            <hr>

        </main>
        <%- include('../includes/end.ejs') %>